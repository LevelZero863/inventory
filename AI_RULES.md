# AI 开发安全规则 V1.0.1

1. 禁止删除、覆盖或重建正式数据库。
2. 禁止 DROP DATABASE / DROP TABLE，除非用户明确批准。
3. 禁止为了通过测试删除既有业务数据。
4. 不得修改历史 migration；数据库结构变化必须新增 migration。
5. 不得把 data/*.db、backups/、.venv/ 提交到 Git。
6. 开发阶段使用开发数据库，不直接操作正式数据库。
7. 正式数据库变更前必须备份，变更后必须执行 integrity_check。
8. 每次功能变更必须增加或更新自动化测试。
9. 测试通过并经人工确认后才允许进入正式环境。
